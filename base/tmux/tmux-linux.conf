# bind -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "xsel -i --clipboard"

bind -Tcopy-mode-vi C-j               send -X copy-pipe-and-cancel 'xsel -i -p'
bind -Tcopy-mode-vi Enter             send -X copy-pipe-and-cancel 'xsel -i -p'
bind -Tcopy-mode-vi MouseDragEnd1Pane send -X copy-pipe-and-cancel 'xsel -i -p'
