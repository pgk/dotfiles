#!/usr/bin/env bash
set -e


# thisdir="$HOME/dotfiles"
# mkdir -p "$HOME/.config/i3"
# mkdir -p "$HOME/.config/i3status"

# ln -sf "$thisdir/install/linux/xinitrc" "$HOME/.xinitrc"
# ln -sf "$thisdir/install/linux/t430/xbindkeysrc" "$HOME/.xbindkeysrc"
# ln -sf "$thisdir/install/linux/xmodmap-esc" "$HOME/.xmodmap-esc"
# # ln -sf "$thisdir/install/linux/xsessionrc" "$HOME/.xsessionrc"
# ln -sf "$thisdir/install/linux/xmodmap" "$HOME/.xmodmap"
# ln -sf "$thisdir/install/linux/Xresources" "$HOME/.Xresources"
# ln -sf "$thisdir/.config/i3/config" "$HOME/.config/i3/config"
# ln -sf "$thisdir/.config/i3status/config" "$HOME/.config/i3status/config"

# chmod +x "$HOME/.xinitrc"

# gsettings set org.gnome.desktop.peripherals.keyboard repeat-interval 25
# gsettings set org.gnome.desktop.peripherals.keyboard delay 150
# ln -sf "$HOME/.xinitrc" "$HOME/.xsession"
# ln -sf "$HOME/.xinitrc" "$HOME/.xsessionrc"

# sudo cp -f "$thisdir/install/linux/lightdm.custom.session" "/usr/share/xsessions/custom.session"
